#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t, n,a;
    cin >> t;
    for (int i = 1; i <= t; i++)
    {
        cin >> n;
        for (int op=1; op <= n; op++)
        {
        scanf("%d",&a);
        }
    }
}